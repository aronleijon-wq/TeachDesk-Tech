# Roadmap

- [x] Update site titles & meta descriptions — user chose to keep current values; added sitewide default title + description in src/routes/__root.tsx (pages with their own keep them; og: tags already mirror).
